#include "Sol.h"
void Sol::update(float deltaTime, sf::Event* event)
{
    
}
void Sol::interact(Player& player, sf::Event* event)
{
    
}

void Sol::draw(sf::RenderWindow* window)
{
    window->draw(sprite);
}